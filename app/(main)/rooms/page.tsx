import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function RoomsPage() {
  return (
    <div className="container mx-auto p-6">
      <Card>
        <CardHeader>
          <CardTitle>Quản lý Phòng chiếu</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Nội dung quản lý phòng chiếu sẽ được phát triển ở đây</p>
        </CardContent>
      </Card>
    </div>
  );
}